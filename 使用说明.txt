
1, 运行"run.bat", 
    - 输入"main.exe listports", 列出本机所有串口.
    - 输入"main.exe 串口号", 运行程序,并打开指定串口(例如: main.exe com1).
    - 也可直接点击main.exe, 不带参数运行, 此时只能再如已保存的报文记录,不能进行串口相关操作.

2, 可以通过config.ini, 进行其他配置, 具体参见config.ini注释.